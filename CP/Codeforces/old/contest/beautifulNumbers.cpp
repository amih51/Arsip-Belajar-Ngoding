#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int mod = 1000000007;


int main(){
    ll a, b, n; cin >> a >> b >> n;
    ll max = 0;
    vector<ll> arr;
    for(int i = 0; i < n; i++){
        max += b * pow(10,i);
    }
    for(int i = 0; i < n; i++){
        
    }
}
